import React from "react";

type Props = {};

export const Header = (props: Props) => {
  return (
    <header className="w-full h-[calc(100vh-8rem)] px-32 bg-headerImage bg-cover bg-center header-bg bg-blend-multiply flex justify-start"></header>
  );
};
